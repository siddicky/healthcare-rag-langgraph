---
type: "Directory Guide"
title: "openwiki/operations/"
description: "Directory guide to local development operations and the production deploy pipeline: uv setup, operating the local Weaviate vector store, ingesting corpus chunks, running the CLI, and the prod-only Fly deploy path."
---

<!-- Parent: ../AGENTS.md -->
<!-- Generated: 2026-08-22 | Updated: 2026-08-22 -->

# openwiki/operations/

## Purpose
Documents both local development operations and the production deploy pipeline: setting up with
`uv`, running/recovering the local Weaviate vector store, ingesting corpus chunks, running the CLI,
and the prod-only Fly deploy (compliance gate, GitHub Environment secrets, immutable-digest tag
pipeline, Weaviate companion app, prod ingest, synthetic-account smoke test).

**Generated by OpenWiki** — do not hand-edit; see `../AGENTS.md`.

## Key Files
| File | Description |
|------|-------------|
| `runbook.md` | Local dev: `uv` setup, operating the local vector store, ingesting corpus chunks, running the CLI, and safe recovery steps. |
| `deploy.md` | Production Fly deploy: compliance gate, GitHub Environment `production` as the single secrets source, `.github/workflows/deploy.yml` tag pipeline, Weaviate companion app, prod ingest, deployed synthetic-account smoke. Summarizes `docs/deploy.md`. |
| `index.md` | Auto-generated listing of this directory's pages. |

## For AI Agents

### Working In This Directory
Regenerate via `make wiki-update` after changing the Makefile, `deploy/`, or
`.github/workflows/deploy.yml`; do not edit `runbook.md` or `deploy.md` directly. `deploy.md` is a
summary — `docs/deploy.md` in the repo root is the authoritative runbook for actual releases.

<!-- MANUAL: Notes added below this line are preserved on regeneration -->
