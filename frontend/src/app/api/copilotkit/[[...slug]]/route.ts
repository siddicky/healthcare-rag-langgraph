/**
 * Next.js App Router entry for the CopilotKit runtime. Implementation lives in
 * `@/lib/copilotkit-runtime` (this directory name is glob-hostile for test
 * discovery, so tests import the lib module directly).
 */
export { GET, POST, PATCH, DELETE } from "@/lib/copilotkit-runtime";
