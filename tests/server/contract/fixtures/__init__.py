"""Contract fixtures recorded from pinned oracle (langgraph-api==0.12.6).

Each JSON file captures a request/response pair observed against the real
oracle. The OSS server must satisfy the same assertions (Phase B).
"""
